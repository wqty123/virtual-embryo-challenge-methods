# 踩坑记录（Pitfalls）

> 所有坑按两个维度分类：**方法学/生物学类**（对"怎么建模"的认识）与**工程/流程类**（对"怎么提交/记录"的认识）。
> 每条标注：现象 → 根因 → 教训。分数均来自官方评分返回。

---

## 一、方法学 / 生物学类

### 1. 全局标量假设：shift 族的 de_score 封顶
- **现象**：shift（伪bulk漂移，全基因同幅度缩放）把所有 damp 扫完，de_score 封顶 ≈45.3（48.85 是峰值）。
- **根因**：真实发育变化是 per-gene / per-type **异质**的，一个标量参数描述不了异质性。
- **教训**：de_score 专挑"少数被点名的基因"判对错——用均值平移永远猜不到哪些基因会被点名。真正的瓶颈不是降维丢特征，是预测算法不擅长点名单个基因。

### 2. 时间齐性假设证伪：Markov 通量全线崩
- **现象**：`markov_flux`（把 E8.5→E9.5 学到的转移规则套到 E9.5→E10.5）44.19，低于地板且低于全部 shift 族。
- **根因**：转移规则本身**随时间在变**——E8.5→E9.5 的规则不能搬到下一步。两点数据学不出"规则的变化规律"。
- **教训**：强假设必须配"考试卷"（中间点数据本地伪验证），在花真榜名额之前先检验假设成不成立。

### 3. "生成内容幅度一大，变异性就崩"：S-am 教训
- **现象**：shift+匿名程序混合 47.34，variogram 崩到 31.7，总分低于纯 shift@1.0。
- **根因**：匿名混合把"已知程序的线性组合"当作新内容，幅度一大就毁掉基因-基因协方差结构。
- **教训**：生成内容幅度必须小、位置受控、最后加。per-gene 独立外推同样毁 vario（基因间协方差无约束）。

### 4. 弥散（噪声）证伪
- **现象**：shift@0.5+s0.5（弥散探针）42.29，vario -19、mmd 也掉。
- **根因**：往预测里加噪声直接毁分布结构。
- **教训**：噪声只该用于本地校准和方差分析，不该进预测。

### 5. 凸包困境：程序混合只能"插值旧状态"
- **现象**：所有程序混合类（OT 程序混合、匿名混合）要么低分要么毁 vario。
- **根因**：已知程序的线性组合只能在凸包内采样——生成的新状态本质是旧状态的插值，正是官方判词的反面（seen-bias）。
- **教训**：要想真正生成"新类别/新状态"，必须处理 population composition 的 birth/death，而不是在旧状态里插值。

### 6. "少预测一种类型 = 重罚，不是扣一分"
- **现象**：T1 少一类的提交分数掉得厉害；mmd_u 把两堆细胞当分布比，缺失的一块形成明显空洞。
- **根因**：评分不知道生物学，只知道分布像不像。过滤掉真实存在的新类型，mmd 直接崩。
- **教训**：细胞类型时间窗先验只能**反着用**（"某些类型不该出现时过滤掉"），不能正向用（"只允许已知类型出现"）——而我们只有一对时间点，连"消失规律"都证明不了。

### 7. 加 marker 轴是错的（三个用户想法的挑刺结论）
- **现象**：想用"一个细胞最特殊的基因"作为维度/标记线对应细胞类型。
- **根因**：① 冗余——真 marker 通常也是高方差基因，信息已被 PCA 前几主成分吃进；② 距离空间被主观选择扭曲→假聚类；③ 用类型知识定义的距离去"验证"类型预测=循环论证。
- **教训**：不要"加轴"，要"换目标函数"（监督投影 LDA 式）或"在预测阶段给 marker 基因更高权重"。

### 8. "两点定直线，不定曲线"
- **现象**：想拟合细胞生长曲线方程再外推（E8.5、E9.5 两个点）。
- **根因**：n 个点最多确定 n−1 阶多项式；两个点确定一条直线，确定不了曲线。
- **教训**：少数据点下，任何"曲线外推"都只是拍脑袋；"线性 vs 指数"是伪矛盾（转移率线性、数量照样指数增长，Kendall 1948 出生-死亡过程）。

### 9. T2 extrap 板：改形态 = 死
- **现象**：所有动形态/几何的外推提交全部崩盘——`r6b70a20` 38.97、`comp_c100` 32.8、`vmorph_s120` 46.8、`cpfx`（d2_shape 5.7）52.12；而**只动 scale/构成**的 `scale_only` 54.51 稳居最佳、`w005` 54.43 第二。
- **根因**：外推目标阶段（E9.5 之后）的空间结构无法从 E8.25/E8.75 可靠外推，动形态必然被 d2_shape/occupancy_dice 重罚。
- **教训**：extrap 板唯一正确路线 = **分布保真优先**（scale 满档 + 各指标均衡），别碰形态。

### 10. 本地代理不可信：proxy 与真榜脱节
- **现象**：markov 本地 proxy 0.964（看似安全）→ 真榜 44.19；T2 几何指标本地排序与真榜**相反**（v1replica 本地 89.7 > bgm 71.1，真榜 50.9 < 57.8）；nh/occ 本地三点校准 R²=0.025。
- **根因**：本地 proxy 目标（E9.5）与真榜目标（E10.5）结构不同；几何指标对 scorer 种子/采样极敏感。
- **教训**：**本地实验只能排除坏候选，不能选好候选**。真榜才是唯一裁决；"可讲当可验证"是代理自己的错误。

### 11. scorer 噪声底：真榜回填必须带噪声带
- **现象**（vecbench 集成后标定）：同一 champion 文件、6 个 scorer seed，de/dir/severity **sd=0（零噪声轴）**，mmd_u ±5.75 skill 分、variogram ±2.20、energy ±2.27。
- **教训**：mmd/vario 单点差 <3 分不构成证据；de/dir/sev 的差异是决定性的。此前"噪声级"判断缺乏仪器标定。

### 12. 结构保护 ≠ 正确转移方向（covsafe 真榜掉分）
- **现象**：`t1_covsafe_mm_n1249_l25` 48.09 vs 生产参考 shift@2.5 48.85——只改善 variogram（+16.3），de/dir/mmd 全降，headline -0.76。
- **根因**：保护结构（协方差安全化）压低了真实 transition。
- **教训**：**保护结构不能代替正确的未来阶段转移方向**。T1 的 covsafe/appearance/parent-map/sparse-rank/structure-transfer 全部关闭。

### 13. 小面板 OOF 结论不能外推到 32285 面板（kp7 教训）
- **现象**：kp7 成熟度加权抽样曾被 500-gene 面板 OOF 否决，但真榜 +2.30（53.67）。
- **根因**：小面板泛化结论不适用于完整 32,285-gene 面板。
- **教训**：OOF 面板口径必须与真榜一致；(K,c) 排序只能靠真榜，结构轴没有可靠的本地仲裁（F1/F3 符号翻转）。

### 14. 深度生成路线：监督不足而非概念否定
- **现象**：VGFM / CVAE / diffusion（含残差版、carrier bypass、FiLM gate 等十余个变体）在 T2 ladder 上全部未稳定超过 copy-last。
- **根因**：当前可见 ladder 对未来新状态的**监督不足**；继续盲扫 epoch/hidden/K/noise 无信息增益。
- **教训**：不是"深度生成"被否定，是数据监督的边界先到了。继续堆模型不如新增独立数据。

### 15. 最大的可利用缺口 = population composition / state support
- **现象**：Tyser 可识别性分解：mean-only oracle 降 RMSE 但毁分布；mean-direction/carrier-residual 有限；**composition oracle 对 variance correlation 和 sliced Wasserstein 的上界最明显**。
- **根因**：deployable 方法没有足够独立证据恢复这些 oracle 信息（缺真实重复样本与可验证出生事件）。
- **教训**：最大缺口是"哪些细胞类型会出现/消失"，不是表达值本身。

### 16. T3 transfer 家族全部证伪
- **现象**：全局 Δ（KO@E9.5 − WT@E9.5 应用到 E8.75 carrier）40.98，vario 49.8→12.2；cardiac 限制 44.36/44.85（分布保住但 de 掉到地板以下）；文献先验（Mab21l2 靶基因）45.78 与地板平齐。
- **根因**：Mab21l2 衍生响应源与 Gata4 的真实响应**反相关**；"响应信号"在表达通道上不可移植。
- **教训**：T3 必须从真实 KO 数据推断响应（mix 混合法），单靠文献/transfer 死路。

### 17. T3 指标杠杆：severity_slope 才是大头
- **现象**：cmp 系把 de/sev 打到 56.3/97.0 后总分破 64.6 平台；mix 系多混真实细胞换 mmd 但 sev 上不去。
- **根因**：T3 权重 de 0.30 + sev 0.25 = 0.55，mmd 0.12 + vario 0.08 = 0.20。
- **教训**：**精准打 KO 效应（de+sev）优先于分布保真（mmd+vario）**——与 T2 extrap 完全相反。

### 18. 参数扫描结论（多个家族，按真榜确定最优参数）
- `kb` 族：k=65 最优（66.36），k=75 五项全线下滑（vario -3.0）——k 增大方向错误。
- `cmp` 族：b 值低侧更优，b=0.55（65.31）> b=0.70（64.93）> b=1.00（63.35，五项全低于 b070）。
- `kp7_m`：b=0.50 优于 b=0.00 对照 2.30 分（de -4.1 / dir -3.2 / mmd -1.7）——b 是 kp7 的关键参数。
- `kp3_w`：a=0.25 最优；raw 化变体全败、aonly 去掉 b500c15 组件后三线全掉。
- `sel` 族全崩（selpc1 最低 30.21，mmd 15.7/vario 19.6 双崩）；`26 基因 pk/ctl` 族确认关停；`koq` 失败；`vs` 族上限 50.67。
- **教训**：真榜是唯一仲裁；"看起来更复杂的组件"常常是负优化。

### 19. 评分器对"旋转"敏感（社区发现，veckit#7）
- **现象**（intuition-lab）：纯刚体旋转会改变 sliced_wasserstein / occupancy_dice（PCA handedness 问题）。
- **教训**：T2 几何指标的微小变化可能毫无生物学意义；操作坐标前先确认指标对什么敏感。

---

## 二、工程 / 流程类

### 1. 重复提交烧名额（多次发生）
- copy5118 同文件双传、mix70_ko 重投、heart extrap v3 同 SHA-256 三次重传、ancestor_donor_birth 双传、dirprop 疑似改名重传（逐项完全同分）、otmix/shift 早期双传。
- **教训**：上传前必须过 **hash 防重 gate**（t1_/t3_pre_upload_gate + slot_ledger）；portal 每日名额有限（T1 8/8、T2 3/8、T3 6/8，UTC 00:00 重置）。

### 2. 提交格式被拒（三起典型）
- `cpf015r`：9000 cells 超本板 583–5000 上限（rejected）。
- `t2_embryo_tls2n5000` 误投 T1 板：32285 vs 498 基因面板不匹配（rejected）。
- heart interp：500 vs 498 基因，缺 Casp4/Pnliprp1（rejected）。
- **教训**：提交前逐元素核对官方面板顺序（T1 32285 / embryo 498 / heart 500）；细胞数别贪多（>2000 cells 会被最重指标在测量前丢弃，更大只增内存不增分）。

### 3. 台账/文档滞后与错误
- t1_slot_ledger 只记到 P5，缺 E4b 六份；README 写 "not uploaded" 实际已上传；UPLOAD_SHEET 文件名挂错（damp0.75 从未上传，damp0.25 记成 P2@0.5）。
- **教训**：**相信盘，别信记忆**；每轮开工先跑 thread_start_gate 从盘上重载。

### 4. 台账插行破坏合并区（两度踩坑）
- 用 `insert_rows` 会破坏合并单元格；写 `MergedCell` 抛 read-only；两步内存平移依赖中间落盘态会覆盖汇总区。
- **教训**：正确做法 = backup-fill：拆待顺延合并区 → 从快照整体平移值+样式（`copy.copy`）→ 新区 re-merge → clone 上一行样式写新值 → 独立回读脚本对照快照 diff=0。

### 5. 数据/脚本工程错误（EXPERIMENT_REPORT 修复清单）
- T1/T2 混合求交错误降到 500 genes（应分 cohort 对齐回映射到 32,285）。
- proxy copy baseline 二次随机重采样造成虚假胜出（修复后虚假 joint wins 消失）。
- section residual 与 global 相同（section 与 sample 一对一绑定，无 nested replicate，不可识别）。
- adapter uncertainty 方向反了（`se2/(se2+scale)` 应为 `scale/(se2+scale)`，方差越大权重越高是反的）。
- sign support 错误放行 50/50 split（取正/负最大值→应只取与 aggregate sign 一致的 block 比例）。
- 旧 test3 文件误报 pending（历史副本未在 manifest，审计排除并发 warning）。
- 聚合 JSON 被覆盖（run_otmix_sweep aggregate-JSON overwrite 根因已修）。
- 远端 launcher 缺可选工具、嵌套 heredoc 引号错误。
- T2/T3 released h5ad 重复 observation names（只在内存视图 make_unique，不改源数据）。
- obs x/y/z 曾错当首选 spatial 坐标（应优先 X_spatial，两套坐标单独报告）。

### 6. 上下文管理
- 几十 MB 原始导出全读进来会上下文混乱（"上下文过多混乱"的教训）。
- **教训**：只读核心结论段，细节留在盘上按需查；用摘要文件。

### 7. 数据合规纪律（避免踩线的坑）
- 未读取/未使用 held-out 阶段数据；near-E9.5 候选（GSE76118/GNomEx/GSE210521/Feng2022）未下载，等待 accession 级规则确认或排除。
- GSE278603 共同基因仅 15,669（47.06%），只能做 early spatial 先验，不能补 T1 expression。
- 外部数据若与 held-out 更近则视为 held-out（midpoint 边界）——**nearness counts，阶段标签不是测试**。
- **教训**：任何外部来源随提交在 method summary 披露（未披露即违规）；拿不准先问组织者。

---

## 三、一句话教训汇总

1. **方法不是"谁比谁高级"，而是对变化本质的假设强度不同**——假设越强理论上限越高，但单点失效风险越大。
2. **本地 proxy 只能排除坏候选，不能选好候选；真榜是唯一裁决。**
3. **真榜差异 <2×噪声带 视为平局**（mmd/vario ±3~6 分；de/dir/sev 零噪声）。
4. **每块板有各自的关键指标**：T1 是 variogram 结构 + de_score；T2 extrap 是 scale_log_ratio + 形态禁区（d2_shape/occupancy_dice）；T3 是 severity_slope + de_score。
5. **明确证伪的四条死路**（Markov 时间齐性、匿名混合、transfer 家族、形态外推），后续不再重复投入真榜名额。
6. **纪律比单次方法更重要**：预注册、挑刺、证伪、换通道不降级、hash 门禁、盘上记忆——这些流程长期复用，任何单次方法都替代不了。
